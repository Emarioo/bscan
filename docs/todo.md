
- [ ] Hotreloading for VR Tracker Driver. bscan can be recompiled but not the driver since Steam VR locks the dll.
      The hotreloading part would be positioning and bscan code. The driver interface classes and tracker creation is not hotreloaded.
      Tracker device stays the same between reloads.
- [ ] 
